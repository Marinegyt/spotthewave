<div class="feed-card">
  <%= cl_image_tag instance.follower.photo.key, class:'avatar' %>
    <div class="feed-content">
      <span class="text-small text-blue-500">Il y a <%= time_ago_in_words(instance.created_at) %></span>
      <p><span class="bold"><%= instance.follower.nickname %></span> vous suit !</p>
    </div>
</div>

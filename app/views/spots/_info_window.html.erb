<%= link_to spot_path(spot) do %>
  <div class="card card-product card-shadow card-spot-popup">
    <%= cl_image_tag spot.photo.key, class:"card-spot-photo-small" %>
    <div class="card-product-infos">
      <h3><%= spot.name %></h3>
      <div class="d-flex justify-content-between ">
        <p class="mb-2 text-blue-600"><%= spot.city %></p>
        <p class="mb-2 text-nowrap "><strong><%= Review.average_rating(spot) %> <i class="fa-solid fa-star" style="color: #FFD43B;"></i></strong></p>
      </div>
         <div class="weather-over info-window">
            <span class="bold"><%= spot.weather.first["temp_c"] %> C°</span>
            <%= image_tag spot.weather.first["condition"]["icon"] %>
                                  <%# WARNING => TO UNCOMMENT
                      WARNING => TO UNCOMMENT
                      WARNING => TO UNCOMMENT
                      WARNING => TO UNCOMMENT %>

            <span class="bold"> <%= spot.marine_weather.first["waveHeight"]["noaa"] %>m</span>
          </div>
    </div>
  </div>
<% end %>
